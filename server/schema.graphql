type Query {
  users: [User!]!
  posts: [Post!]!
  user(id: String): User
  post(id: String): Post
}

type Mutation {
  register(input: registerInput): User
  createPost(body: String!): Post
}

type User {
  id: ID!
  name: String!
  password: String!
  posts: [Post!]!
}

type Post {
  id: ID!
  body: String
  author: User
}

input registerInput {
  name: String!
  password: String!
}
